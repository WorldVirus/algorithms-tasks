
function kaprekarNumbers(p, q) {
    let answer = [];

    for(let p; p <= q; ++p){
        if(p === 1 || p === 9){
            answer.push(p)
        }
        let powValue = String(p*p);
        if (powValue.length %2 !== 0 ){

        }
        else{
            let secondPart = powValue.substring(powValue.length,powValue.length+1)
            let secondPart = powValue.substring(powValue.length,powValue.length+1)

        }
    }

}
